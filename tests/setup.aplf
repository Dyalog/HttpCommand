r←setup _;home
r←0
⎕IO ⎕ML←1
:Trap 0
 home ← ##.TESTSOURCE
 #.⎕FIX'file://',home,'../source/HttpCommand.dyalog'
 
 #.CHECK ← { ⍺≡⍵:0 ⋄ 'Expected [',(⍕⍺),'] got [',(⍕⍵),']' }

 #.url ← 'httpbin.org'

 HttpServer.Run 8090 (home,'server/localhost/')
:Else
 r←⎕DMX.DM
:End